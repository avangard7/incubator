import s from './Settings.module.css'; 

const Settings = (props) => {
    return (
        <div className={ s.aries }>
            <h1>Settings</h1>
        </div> 
    )
}

export default Settings; 
