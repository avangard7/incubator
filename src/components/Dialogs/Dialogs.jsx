import s from './Dialogs.module.css'; 

const Dialogs = (props) => {
    return (
        <div className={ s.aries }>
            <h1>Dialogs users</h1>
        </div> 
    )
}

export default Dialogs; 
